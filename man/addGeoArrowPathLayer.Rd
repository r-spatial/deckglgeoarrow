% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addGeoArrowPathLayer.R
\name{addGeoArrowPathLayer}
\alias{addGeoArrowPathLayer}
\title{Add Deck.gl PathLayer to a \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}} map
using blazing fast \code{\link[nanoarrow:read_nanoarrow]{nanoarrow::write_nanoarrow()}} data transfer.}
\usage{
addGeoArrowPathLayer(
  map,
  data,
  source,
  file,
  url,
  layer_id = "path",
  geom_column_name = "geometry",
  popup = NULL,
  tooltip = NULL,
  render_options = renderOptions(),
  data_accessors = dataAccessors(),
  popup_options = popupOptions(),
  tooltip_options = tooltipOptions(),
  ...
)
}
\arguments{
\item{map}{the \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}} map to add the layer to.}

\item{data}{a \code{sf}, \code{wk}, \code{geos} or \code{SpatVector} \verb{(MULTI)LINESTRING} object.
Ignored if \code{source} is supplied.}

\item{source}{the \code{id} of a source previously added via \code{\link[=addSource]{addSource()}}.}

\item{file}{a valid local file path to a \code{geoarrow} or \code{geoparquet} file to be
added to the map. Ignored if \code{data} is supplied.}

\item{url}{a URL to a remotely hosted \code{geoarrow} or \code{geoparquet} file to be
added to the map. Ignored if \code{data} or \code{file} is supplied.}

\item{layer_id}{the layer id.}

\item{geom_column_name}{the name of the geometry column of the data object.
It is inferred automatically if only one is present.}

\item{popup}{should a popup be contructed? If \code{TRUE}, will create a popup fromm all
available attributes of the feature. Can also be a character vector of column
names, on which case the popup will include only those columns. If a single character
is supplied, then this will be shown for all features. If \code{NULL} (deafult) or
\code{FALSE}, no popup will be shown.}

\item{tooltip}{should a tooltip be contructed? If \code{TRUE}, will create a tooltip fromm all
available attributes of the feature. Can also be a character vector of column
names, on which case the tooltip will include only those columns. If a single character
is supplied, then this will be shown for all features. If \code{NULL} (deafult) or
\code{FALSE}, no tooltip will be shown.}

\item{render_options}{a list of \link{renderOptions}}

\item{data_accessors}{a list of \link{dataAccessors}}

\item{popup_options}{a list of \link{popupOptions}}

\item{tooltip_options}{a list of \link{tooltipOptions}}

\item{...}{can be used to pass additional props and parameters to the deck.gl
instance. See Details for more info.}
}
\value{
The modified \code{map} object with the added path layer.
}
\description{
Add Deck.gl PathLayer to a \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}} map
using blazing fast \code{\link[nanoarrow:read_nanoarrow]{nanoarrow::write_nanoarrow()}} data transfer.
}
\details{
\code{...} can be used to pass additional props and parameters to the deck.gl instance
for fine-tuning rendering behaviour. For example, we can pass a list called
\code{parameters} with settings that control the GPU pipeline of the deck.gl instance.
See \url{https://luma.gl/docs/api-reference/core/parameters} for a list of
available prarmeters.

By default, all deck.gl layers passed to a \code{maplibre()} map will be drawn on
top of existing ones. It is, however, possible to inject layers into the
existing \code{maplibre} (base) layer stack by using
\code{render_options = renderOptions(beforeId = "<some-existing-layer-id>")}
which will plot the current layer underneath \code{"<some-existing-layer-id>"}.
See \link{addGeoArrowScatterplotLayer} for an example.
}
\examples{
library(wk)
library(mapgl)

style_positron = "https://basemaps.cartocdn.com/gl/positron-gl-style/style.json"

m = maplibre(style = style_positron)

## single wk LINESTRING
ln = wkt("LINESTRING (30 10, 10 30, 40 40)")

m |>
  addGeoArrowPathLayer(
    data = ln
    , data_accessors = dataAccessors(
      getColor = "#ff000080"
      , getWidth = 3
    )
  )

## remote parquet file
## paste url together so CRAN check doesn't complain
base_url = "https://raw.githubusercontent.com/geoarrow/"
data_url = "geoarrow-data/v0.2.0/example/files/example_linestring_native.parquet"
url = paste0(base_url, data_url)

m |>
  addGeoArrowPathLayer(
    url = url
    , geom_column_name = "geometry"
    , data_accessors = dataAccessors(
      getColor = "#0000ff90"
      , getWidth = 5
    )
    , popup = TRUE
    , tooltip = TRUE
  )

}
