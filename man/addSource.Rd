% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addSource.R
\name{addSource}
\alias{addSource}
\title{Add a data source to a \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}} map.}
\usage{
addSource(map, id, data, file, url, ...)
}
\arguments{
\item{map}{the \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}} map to add the
data source to.}

\item{id}{a unique \code{ID} for the data source.}

\item{data}{a \code{sf}, \code{wk}, \code{geos} or \code{SpatVector} object.}

\item{file}{a valid local file path to a \code{geoarrow} or \code{geoparquet} file to be
added to the map. Ignored if \code{data} is supplied.}

\item{url}{a URL to a remotely hosted \code{geoarrow} or \code{geoparquet} file to be
added to the map. Ignored if \code{data} or \code{file} is supplied.}

\item{...}{currently not used.}
}
\value{
The modified \code{map} object with the added data source attached.
}
\description{
Add a data source to a \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}} map.
}
\details{
See \code{\link[geoarrowWidget:attachData]{geoarrowWidget::attachData()}} for details.
}
\examples{
library(mapgl)
library(wk)

pt = wkt("POINT (0 0)")

m = maplibre()

m = m |>
  addSource(
    id = "pt"
    , data = pt
  ) |>
  addGeoArrowScatterplotLayer(source = "pt")

## open the map in the browser, press <Ctrl+u> and look for a line like this:
## <link id="pt-geoarrowWidget-attachment" rel="attachment" href="lib/pt-0.0.1/pt.arrow"/>
m

}
